import 'package:flutter/material.dart';

class BecaAlimentacionScreen extends StatelessWidget {
  const BecaAlimentacionScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
